(function($){
    $(document).ready(function() {
        var $body = $('body');

        // if( $body.hasClass('post-type-post') ) {
        //   $("#adminmenu > li").each(function(){
        //     if($(this).find('a').attr('href') == 'edit.php'){
        //       $(this).addClass("wp-has-current-submenu").removeClass("wp-not-current-submenu");
        //       $(this).children('a').addClass("wp-has-current-submenu").removeClass("wp-not-current-submenu");
        //     }
        //   });
        // }

        // if( $body.hasClass('post-type-applications') || $body.hasClass('post-type-page') || $body.hasClass('post-type-sectors') || $body.hasClass('post-type-newsletter') ){
        //     $('.add-new-h2').remove();
        // }

        // if($body.hasClass('post-type-page') || $body.hasClass('post-type-sectors') ){
        //   $('.trash').remove();
        //   $('#delete-action').remove();
        // }

        // if($body.hasClass('post-type-applications') && !$body.hasClass('taxonomy-applications_state') ){
        //   var name = $(".buro-link-name").find("input").attr("value");
        //   var link = $(".buro-link").find("input").attr("value");


        //     $("#postbox-container-2").find($('input')).attr('disabled', 'disabled');


        //   $(".buro-link").find(".acf-input").html("<a target='_blank' href='" + link + "'>" + name + "</a>");
        // }

        // if($body.find('.acf-postbox .hndle')){
        //   $('.acf-hndle-cog.acf-js-tooltip').remove();
        //   // $('.acf-postbox .hndle').css('color', 'transparent');
        // }

        // if($body.hasClass('post-type-newsletter') ){
        //   $('.row-actions').remove();
        // }

        // if($("#wp-admin-bar-my-account > a").text() != "Olá, we@burocratik.com!")
        //   $(".wp-submenu li a").each(function(){
        //     if($(this).attr("href") == "edit.php?post_type=sectors&page=acf-options-mapa-de-paises" )
        //     $(this).parent().hide();
        //   });

        // if( !$body.hasClass('post-type-post') && !$body.hasClass('post-type-jobs') && !$body.hasClass('post-type-applications') && !$body.hasClass('post-type-academy') && !$body.hasClass('post-type-testemonials') ) {
        //   $(".trash").remove();
        // }

    });
})(jQuery);